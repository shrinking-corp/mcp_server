[project]
name = "shrinking-mcp-server"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp",
    "pydantic",
    "shrinking-algorithms @ git+https://github.com/shrinking-corp/algorithms.git@new_parser",
]
